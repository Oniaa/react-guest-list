/* import React, { Fragment, useState } from 'react';

export default function GuestsList() {
  const [guestsL, setGuestsL] = useState(guestsL);

  return (
    <div>
      {guestsL.map((guestL) => {
        return (
          <Fragment key={`guest-profile-${guestL.id}`}>
            <span>
              {guestL.firstName} {guestL.lastName}{' '}
            </span>
          </Fragment>
        );
      })}
      <h2>Guest</h2>
    </div>
  );
}
 */
